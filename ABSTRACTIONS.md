# Abstractions

Tracks reusable infrastructure abstractions ported from Cuida and arive into sloopquest.

## To port from Cuida

- Actions framework
- Row-level security (RLS) mixins, comparator, alembic operations
- CRUD primitives
- API client codegen
- Terraform modules
- LLM client abstraction
- State machine primitives
- Async workers (SAQ wiring)
- ~~Email pipeline (React Email → Jinja2 → SES)~~ ✓

## To port from arive

- Threads + WebSocket channels
- File upload primitive
- Events framework
