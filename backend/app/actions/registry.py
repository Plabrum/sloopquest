"""ActionRegistry — placeholder until the actions framework is ported (SLQ-11)."""


class ActionRegistry:
    """Stub registry. Real implementation arrives with the actions framework."""

    def __init__(self) -> None:
        self._actions: dict[str, object] = {}
