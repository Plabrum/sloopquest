/**
 * Generated by generate-columns.ts — DO NOT EDIT
 * Source: list_Engine operation
 */
import { createColumnBuilder } from "@/lib/column-builder";
import type { EngineListItem } from "@/openapi/litestarAPI.schemas";
import { EnginePosition, FuelType, EngineType } from "@/openapi/litestarAPI.schemas";

export const engineColumnDefs = createColumnBuilder<EngineListItem>()
  .enum("position", { header: "Position", options: Object.values(EnginePosition) })
  .text("model", { header: "Model" })
  .text("serial_number", { header: "Serial Number" })
  .number("year", { header: "Year" })
  .number("horsepower", { header: "Horsepower" })
  .enum("fuel_type", { header: "Fuel Type", options: Object.values(FuelType) })
  .enum("engine_type", { header: "Engine Type", options: Object.values(EngineType) })
  .number("hours_at_survey", { header: "Hours At Survey" })
  .datetime("created_at", { header: "Created", sortable: true, hideOnMobile: true })
  .build();

export const EngineSortableColumn = {
  created_at: 'created_at',
} as const;

export const EngineFilterableColumn = {
  vessel_id: 'vessel_id',
} as const;
