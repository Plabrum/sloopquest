/**
 * Initial stub — replaced by `pnpm codegen` (generate-forms.ts) once the
 * backend exposes /schema/action-metadata. Tracked in git so the action
 * registry can merge it without an explicit codegen step in CI.
 */
import type { ReactElement } from "react";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const generatedRegistry: Record<string, { render: (params: any) => ReactElement }> = {};
